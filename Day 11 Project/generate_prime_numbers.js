function generatePrimeNumber(num)
{
    let x = 1;
    let count = 0;

    while(x <= num)
    {
        if(num % x == 0)
        {
            count++;
        }
    }

    if(count == 2)
    {
        return true;
    }
}

let a = 1;
let b = 0;

while(a <= 5)
{
    if()
}

generatePrimeNumbers(5);